import React from "react";
import { View, Text, StyleSheet, Image, Button } from "react-native";

const Margin = (props :{height:any}) => {
    return (
        <View style={{height:props.height}}></View>
    );
};

export default Margin;